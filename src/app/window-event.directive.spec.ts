import { WindowEventDirective } from './window-event.directive';

describe('WindowEventDirective', () => {
  it('should create an instance', () => {
    const directive = new WindowEventDirective();
    expect(directive).toBeTruthy();
  });
});
